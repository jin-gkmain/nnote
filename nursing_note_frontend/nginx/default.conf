# =====================================================
# Nginx ���� - ��ȣ��� ����
# 1) ����Ʈ���� ���� ���� ���� (Vite ���� �����)
# 2) /api ��û�� �鿣�� NestJS �����̳ʷ� ���Ͻ�
# =====================================================

server {
    listen 80;
    server_name _;

    # ����Ʈ���� ���� ����� ��ġ
    root /usr/share/nginx/html;
    index index.html;

    # OCR �̹��� ���ε� ���� ���� ��û ���� ũ�� ���� Ȯ��
    client_max_body_size 20M;

    # /api ��û �� �鿣�� NestJS �����̳ʷ� ���Ͻ�
    # docker-compose ��Ʈ��ũ���� ���񽺸� "backend"�� ����
    location /api/ {
        proxy_pass http://backend:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        # Apache�� HTTPS�� ���� ��� �� ���� �鿣����� ���� ($scheme�� ���� ���Ͻ� ������ HTTP�� http�� �Ѿ)
        proxy_set_header X-Forwarded-Proto $http_x_forwarded_proto;

        # STT �� ��ð� API: �⺻ proxy_read_timeout(60s)�� �鿣�� STT(�ִ� 180s)���� ª���� 504
        proxy_connect_timeout 60s;
        proxy_send_timeout 600s;
        proxy_read_timeout 600s;
    }

    # WhisperLiveKit real-time STT WebSocket proxy.
    # The frontend streams MediaRecorder chunks to /asr while recording.
    location /asr {
        proxy_pass http://live-stt:8000/asr;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $http_x_forwarded_proto;
        proxy_connect_timeout 60s;
        proxy_send_timeout 3600s;
        proxy_read_timeout 3600s;
    }

    # SPA ����� ó��
    # React Router ���� ����ϴ� Ŭ���̾�Ʈ ���̵� ������� ����
    # ���� ������ ���� ��δ� ��� index.html�� ��������
    location / {
        try_files $uri $uri/ /index.html;
    }

    location = /manifest.webmanifest {
        default_type application/manifest+json;
        try_files $uri =404;
    }

    location = /service-worker.js {
        default_type application/javascript;
        add_header Cache-Control "no-cache";
        try_files $uri =404;
    }

    # ���� ����(JS, CSS, �̹��� ��) ĳ�� ����
    # Vite ���� ������� ���ϸ��� �ؽð� ���ԵǹǷ� ��� ĳ�� ����
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
